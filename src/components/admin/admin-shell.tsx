import Link from 'next/link'
import { BarChart3, Box, ChevronDown, CircleDollarSign, LayoutDashboard, PackagePlus, Printer, Settings, Truck, Users } from 'lucide-react'
const nav=[['Dashboard','/admin',LayoutDashboard],['Shipments','/admin/shipments',Box],['New shipment','/send',PackagePlus],['Pricing','/admin/pricing',CircleDollarSign],['Carriers','/admin/carriers',Truck],['Customers','/admin/customers',Users],['Printers','/admin/settings/printer',Printer],['Settings','/admin/settings',Settings]] as const
export function AdminShell({children,title,subtitle,active='Dashboard'}:{children:React.ReactNode;title:string;subtitle?:string;active?:string}){return <div className="admin-shell"><aside><Link className="brand inverse" href="/"><span className="brand-mark">P</span>Pak & Send</Link><nav>{nav.map(([name,href,Icon])=><Link key={name} className={active===name?'active':''} href={href}><Icon/>{name}</Link>)}</nav><div className="admin-mode"><i/> Development mode<small>Mock rates enabled</small></div></aside><main><header className="admin-header"><div><h1>{title}</h1>{subtitle&&<p>{subtitle}</p>}</div><div className="admin-user"><span>KJ</span><b>Kasper Jensen<small>Administrator</small></b><ChevronDown/></div></header>{children}</main></div>}
export function Status({value}:{value:string}){return <span className={`status ${value.toLowerCase()}`}>{value.replace('_',' ')}</span>}
export function Metric({label,value,change,icon:Icon}:{label:string;value:string;change:string;icon:typeof BarChart3}){return <section className="metric"><header><span>{label}</span><Icon/></header><b>{value}</b><small>{change}</small></section>}
