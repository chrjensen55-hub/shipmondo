import { Printer } from 'lucide-react'
import { AdminShell } from '@/components/admin/admin-shell'
export default function PrinterSettings(){return <AdminShell title="Printer settings" subtitle="Connect Shipmondo Print Client and a label printer" active="Printers"><div className="admin-content"><section className="setup-card"><Printer/><h2>No printer connected</h2><p>Add Shipmondo server credentials, install Shipmondo Print Client on the store computer, then select the configured Zebra printer here.</p><div className="fields two"><label className="field"><span>Label format</span><select defaultValue="ZPL"><option>ZPL</option><option>PDF</option></select></label><label className="field"><span>Printer</span><select disabled><option>Connect Shipmondo to load printers</option></select></label></div><button className="button button-primary">Test connection</button></section></div></AdminShell>}
