import { AdminShell, Status } from '@/components/admin/admin-shell'
import { shipments } from '@/lib/mock-data'
export const dynamic = 'force-dynamic'
export default async function Shipment({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=shipments.find(x=>x.id===id)??shipments[0];return <AdminShell title={s.id} subtitle={s.date} active="Shipments"><div className="admin-content detail-grid"><section className="table-card detail"><header><div><h2>Shipment details</h2><p>{s.customer} → {s.destination}</p></div><Status value={s.status}/></header><dl><dt>Carrier</dt><dd>{s.carrier} {s.service}</dd><dt>Tracking</dt><dd>{s.tracking}</dd><dt>Customer price</dt><dd>{s.sale} DKK</dd><dt>Purchase price</dt><dd>{s.purchase} DKK</dd><dt>Gross profit</dt><dd className="profit">{s.sale-s.purchase} DKK</dd><dt>Margin</dt><dd>{((s.sale-s.purchase)/s.sale*100).toFixed(1)}%</dd></dl></section></div></AdminShell>}
