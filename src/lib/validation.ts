import { z } from 'zod'
const address=z.object({fullName:z.string().trim().min(2),company:z.string().optional(),address1:z.string().trim().min(3),address2:z.string().optional(),postalCode:z.string().trim().min(2),city:z.string().trim().min(2),country:z.string().length(2),state:z.string().optional(),email:z.email(),phone:z.string().trim().min(6),instructions:z.string().max(300).optional()})
const parcel=z.object({id:z.string(),weight:z.number().positive(),length:z.number().positive(),width:z.number().positive(),height:z.number().positive()})
export const quoteRequestSchema=z.object({originCountry:z.string().length(2),originPostalCode:z.string().min(2),destinationCountry:z.string().length(2),destinationPostalCode:z.string().min(2),parcels:z.array(parcel).min(1)})
export const shipmentSchema=quoteRequestSchema.extend({sender:address,recipient:address,contentsType:z.enum(['DOCUMENTS','GOODS']),items:z.array(z.object({id:z.string(),description:z.string().min(2),quantity:z.number().int().positive(),unitValue:z.number().nonnegative(),currency:z.string().length(3),weight:z.number().positive(),originCountry:z.string().length(2),hsCode:z.string().optional()})),exportReason:z.string().optional(),selectedQuoteId:z.string().min(1),confirmation:z.literal(true),idempotencyKey:z.string().min(8)})
