import 'server-only'
import type { ShipmentDraft } from '@/lib/types'
export function mapShipmentToShipmondo(shipment:ShipmentDraft):never{void shipment;throw new Error('TODO: implement only after validating the official Shipmondo API v3 shipment schema')}
